language: node_js
node_js:
  - "node"
cache:
  directories:
    - "node_modules"
env:
  - NODE_ENV=development
install:
  - npm ci
script:
  - npm t
  - npm run build
  - npx codecov
deploy:
  - provider: script
    script: now --token $NOW_TOKEN
    skip_cleanup: true
    on:
      all_branches: true
      master: false
  - provider: script
    script: now --token $NOW_TOKEN && now alias --token $NOW_TOKEN
    skip_cleanup: true
    on:
      master: true
notifications:
  email: false
